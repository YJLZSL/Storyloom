# Checklist

## Phase 1 — 基础设施修复
- [x] package.json 恢复，含完整 scripts/dependencies/electron-builder 配置
- [x] 开发/生产环境分离配置（dev.db vs userData/timeline.db）
- [x] `npm install` 成功
- [ ] `npm run dev` 启动前端 5173 + 后端 3001
- [ ] `npm run build` 构建成功
- [x] Electron 单实例锁实现，重复启动时新实例退出并聚焦旧窗口
- [x] 日志正确序列化 Error 对象（message + stack + code），不再显示 `{}`
- [x] startServer 端口冲突时抛出异常而非 process.exit(1)
- [x] 错误对话框显示完整错误信息（message + stack）
- [x] 动态 import 使用绝对路径并验证文件存在
- [x] 开发模式加载 localhost:5173，生产模式 loadFile
- [x] 字体本地化，不依赖 Google Fonts
- [x] API 204 状态码正确处理，DELETE 操作不再误判为失败
- [x] CORS 开发允许 localhost，生产限制 file://+localhost
- [x] 全局 bodyLimit 降至 5MB
- [x] events 路由所有端点验证 workspaceId 存在性
- [x] LIKE 搜索正确转义反斜杠
- [x] AI 流式请求客户端断开时后端停止写入并中止上游请求
- [x] 崩溃恢复包含 eventWorldSettings 关联数据
- [x] 导出数据包含 eventWorldSettings
- [x] 导入数据包含 eventWorldSettings
- [x] 导入 overwrite 策略先删除再插入
- [x] 导入 merge 策略使用 onConflictDoUpdate
- [x] 导入 skip 策略保持 onConflictDoNothing
- [x] 错误处理器 500 错误记录完整堆栈日志
- [x] AI 降级响应包含 degraded: true 和 error 字段
- [x] ExportData 类型包含 eventWorldSettings 字段

## Phase 2 — 设计系统重写
- [ ] index.css 使用 Tailwind v4 @theme 定义设计令牌
- [ ] 2 主题（素纸亮色/墨黑暗色）CSS 变量使用 RGB 通道格式
- [ ] 专注模式工具类定义
- [ ] 12 主题 + 3 视觉模式 CSS 全部移除
- [ ] View Transitions API 圆形扩散主题切换样式
- [ ] 字体本地化（思源宋体/思源黑体/JetBrains Mono）
- [ ] shadcn/ui 初始化（components.json + cn()）
- [ ] Button/Input/Textarea/Label/Badge 基础组件可用
- [ ] Dialog/Sheet/Tooltip/Sonner 反馈组件可用
- [ ] Command 组件可用
- [ ] Select/Checkbox/Switch/Slider 表单组件可用
- [ ] ScrollArea/Separator/Tabs 数据组件可用

## Phase 3 — 前端架构重写
- [ ] useWorkspaceStore 实现（currentWorkspaceId）
- [ ] useTimelineStore 实现（viewMode/zoom/selectedEventId/scrollToEventId）
- [ ] useUIStore 实现（activePanel/panelWidth/focusMode/commandPaletteOpen）
- [ ] useHistoryStore 保留并正常工作
- [ ] 旧 store（appStore/panelStore/themeStore）逻辑迁移完成
- [ ] AppShell 工作台布局：顶部(48px)+左侧(56px)+主画布+右侧(可拖拽)+底部(28px)
- [ ] TopToolbar 仅保留核心操作
- [ ] SideNav 纯图标导航
- [ ] ContextPanel 上下文感知（选中事件→事件编辑器）
- [ ] StatusBar 显示事件数/字数/保存状态/缩放
- [ ] 右侧面板可拖拽调整宽度（280-480px）
- [ ] 专注模式（F11）正常工作
- [ ] TimelineCanvas 绝对像素定位，缩放真正改变间距
- [ ] TimelineRuler 动态刻度，支持自定义日历
- [ ] 连线使用 SVG 绘制，虚拟滚动下无遗漏
- [ ] 虚拟滚动标尺/事件/连线协调
- [ ] 拖拽改时长功能实现，调用 updateEvent API
- [ ] TimelineEventCard Framer Motion 悬浮抬起，无 3D 倾斜
- [ ] TimelineTrack 移除内部 useTracks，props 传入
- [ ] AI 面板使用 textarea 多行输入
- [ ] AI 回复 Markdown 渲染
- [ ] AI 对话管理（新建/切换/删除）+ localStorage 持久化
- [ ] AI 上下文感知（选中事件自动注入 prompt）
- [ ] AI 请求走后端代理
- [ ] AI 降级提示显示
- [ ] WorkspaceSelector 简化，无粒子/打字机/3D 动画
- [ ] 新建工作区时可选故事结构模板

## Phase 4 — 新增高价值功能
- [ ] 命令面板 Ctrl+K 触发
- [ ] 命令面板搜索事件/角色/世界观/伏笔
- [ ] 命令面板 ">" 前缀执行命令
- [ ] 命令注册表实现（id+title+icon+handler）
- [ ] 快捷键支持 when 上下文
- [ ] 快捷键跨平台适配（Mod 元键）
- [ ] 快捷键设置面板+冲突检测
- [ ] 故事模板库定义（5 种模板）
- [ ] 新建工作区模板选择 UI
- [ ] 应用模板自动生成预设事件轨道
- [ ] 关系图谱视图实现（D3 force simulation）
- [ ] 关系图节点按类型颜色区分
- [ ] 关系图支持拖拽/缩放
- [ ] 关系图点击节点跳转详情
- [ ] SideNav 有关系图视图入口

## Phase 5 — 测试与验证
- [x] `npx tsc -b --noEmit` 无错误
- [x] `npm run build` 成功
- [x] `npm run build:server` 成功
- [x] `npm run build:electron` 成功
- [x] 开发模式：`npm run dev` 启动前端 5173 + 后端 3001
- [ ] 开发模式：`npm run dev:electron` 启动 Electron
- [x] 开发模式：API /api/health 返回 200 OK
- [ ] 开发模式：UI 正确显示工作台布局
- [ ] 开发模式：双主题切换正常
- [ ] 开发模式：时间轴缩放改变间距
- [ ] 开发模式：命令面板 Ctrl+K 可用
- [ ] 开发模式：关系图谱视图可用
- [x] 生产模式：`npx @electron/rebuild -f -w better-sqlite3` 成功
- [x] 生产模式：`npx electron-builder --win --publish never` 成功
- [x] 生产模式：打包后应用启动无错误对话框
- [x] 生产模式：API /api/health 返回 200 OK
- [x] 生产模式：重复启动触发单实例锁
- [x] 生产模式：使用 userData/timeline.db
- [x] 生产模式：`npm rebuild better-sqlite3` 恢复 Node.js 环境

# 开发路线图

> AI Timeline Creator V4.0 — 分优先级开发任务清单
> 最后更新：2026-06-17
> 当前完成度：前端架构 ~85% | 后端 ~90% | 总体 ~80%
> V7 全面重构已完成（5 阶段 17 任务），当前聚焦遗留问题修复

---

## 阅读指引

- `[ ]` 待完成 `[x]` 已完成 `[~]` 部分完成
- 每个任务标注了涉及的文件路径，方便直接定位
- P0 = 阻塞性缺陷必须立即修复, P1 = 核心功能缺失, P2 = 体验提升, P3 = 锦上添花
- ⚠️ V7 重构遗留问题清单详见 [前端问题识别与修复任务](./前端问题识别与修复任务.md)

---

## V7 全面重构（已完成 ✅）

以下任务在 V7 重构中全部完成（2026-06-17）：

### Phase 1 — 基础设施修复 ✅

- `[x]` Task 1: 恢复 package.json 与环境分离配置（dev 用 ./data/dev.db，prod 用 userData/timeline.db）
- `[x]` Task 2: 修复 Electron 启动可靠性（单实例锁、错误序列化、ESM file:// URL import）
- `[x]` Task 3: 修复后端关键 Bug（204 状态码、CORS、AI 流断开、eventWorldSettings 补全等 12 个）

### Phase 2 — 设计系统重写 ✅

- `[x]` Task 4: 重写设计令牌系统（Tailwind v4 @theme、2 主题 RGB 通道格式 CSS 变量、专注模式）
- `[x]` Task 5: 完整集成 shadcn/ui（18 个基础组件，new-york 风格）

### Phase 3 — 前端架构重写 ✅

- `[x]` Task 6: 重组状态管理（4 个域 store：useWorkspaceStore/useTimelineStore/useUIStore/useThemeStore）
- `[x]` Task 7: 重写工作台布局（AppShell + TopToolbar + SideNav + ContextPanel + StatusBar，CSS Grid）
- `[x]` Task 8: 重写时间轴核心（绝对像素定位、SVG 连线、拖拽改时长、虚拟滚动、Framer Motion）
- `[x]` Task 9: 重写 AI 面板（textarea + Markdown 渲染 + 对话管理 + 后端代理 + 降级提示）
- `[x]` Task 10: 重写工作区选择界面（Framer Motion + 模板选择）

### Phase 4 — 新增高价值功能 ✅

- `[x]` Task 11: 命令面板（Ctrl+K，搜索 + 命令执行，基于 shadcn Command）
- `[x]` Task 12: 快捷键系统升级（when 上下文 + 跨平台 Mod 映射 + 设置面板）
- `[x]` Task 13: 故事结构模板库（5 个模板：英雄之旅/三幕式/拯救猫咪/编年体/传记体）
- `[x]` Task 14: 关系图谱视图（D3 force simulation 力导向布局）

### Phase 5 — 测试与验证 ✅

- `[x]` Task 15: 编译与构建验证（tsc + vite build + build:server + build:electron 全部通过）
- `[x]` Task 16: 开发模式验证（前端 5173 + 后端 3001 + API 200 OK）
- `[x]` Task 17: 生产模式打包验证（打包成功，启动无错误，单实例锁正常）

---

## P0 — V7 重构遗留阻塞性问题（必须立即修复）

> 完整问题描述详见 [前端问题识别与修复任务](./前端问题识别与修复任务.md) P0 部分

### P0-1. 接入 RelationshipView 到 MainCanvas

- **文件**：`src/components/layout/AppShell.tsx` 第 36-49 行
- **问题**：MainCanvas 中只有 `viewMode === 'timeline'` 渲染 `<TimelineView />`，其他 5 个视图全是占位符
- **关键**：`RelationshipView` 已完整实现（使用新 store + d3），可直接接入
- `[ ]` relationship → 直接接入 `RelationshipView`（已用新 store）

### P0-2. 迁移并接入 EventEditorDialog 到 ContextPanel

- **文件**：`src/components/events/EventEditorDialog.tsx`（370+ 行）、`src/components/layout/ContextPanel.tsx`
- **问题**：EventEditorDialog 完全未接入，ContextPanel 中 event-editor 是占位符
- **关键**：EventEditorDialog 自身使用旧 store（useAppStore、useThemeStore）和 micro-interactions.ts，需迁移
- `[ ]` 迁移 EventEditorDialog 到新 store
- `[ ]` 接入 ContextPanel 的 event-editor 面板

### P0-3. 迁移并接入 5 个面板到 ContextPanel

- **文件**：`src/components/layout/ContextPanel.tsx` 第 114-144 行
- **问题**：除 ai 面板外，其余全是占位符
- `[ ]` characters → 迁移 CharacterPanel 到新 store 后接入
- `[ ]` worldview → 迁移 WorldBuildingPanel 到新 store 后接入
- `[ ]` foreshadowing → 迁移 ForeshadowingPanel 到新 store 后接入
- `[ ]` connections → 迁移 ConnectionPanel 到新 store 后接入
- `[ ]` consistency → 迁移 ConsistencyPanel 到新 store 后接入（ContextPanel switch 也未处理）

### P0-4. 迁移并接入 4 个视图到 MainCanvas

- `[ ]` outline → 迁移 OutlineView 到新 store 后接入
- `[ ]` narrative → 迁移 NarrativeView 到新 store 后接入
- `[ ]` gantt → 迁移 GanttTimelineView 到新 store 后接入
- `[ ]` statistics → 迁移 StatsView 到新 store 后接入

### P0-5. TopToolbar 新建/保存按钮接入功能

- **文件**：`src/components/layout/TopToolbar.tsx`
- **问题**："新建事件"按钮（Plus 图标）和"保存"按钮（Save 图标）无 onClick
- `[ ]` 新建事件打开 EventEditorDialog（新建模式）
- `[ ]` 保存触发自动保存 API

### P0-6. TimelineCanvas 订阅 scrollToEventId

- **文件**：`src/components/timeline/TimelineCanvas.tsx`
- **问题**：useTimelineStore 有 scrollToEventId 和 scrollToEvent，CommandPalette 调用 scrollToEvent，但 TimelineCanvas 未订阅
- `[ ]` TimelineCanvas 订阅 scrollToEventId，useEffect 中滚动到对应事件

### P0-7. 重新实现 useHistoryManager 并挂载到 AppShell

- **文件**：`src/components/command-palette/commands.ts`、`src/components/history/useHistoryManager.tsx`
- **问题**：handleUndo/handleRedo 只调用 historyStore.undo()/redo()，但不应用返回的 HistoryRecord 到 API；useHistoryManager 是死代码
- `[ ]` 重新实现 useHistoryManager 并挂载到 AppShell
- `[ ]` 在事件/角色等 CRUD 操作中推送历史记录

### P0-8. handleSave 调用实际保存 API

- **文件**：`src/components/command-palette/commands.ts` 第 159-161 行
- **问题**：handleSave 只执行 toast.success('已保存')，不执行实际保存
- `[ ]` 调用后端 auto-save API 保存当前工作区快照

---

## P1 — 清理（删除死代码）

> 完整清单详见 [前端问题识别与修复任务](./前端问题识别与修复任务.md) P1 部分，共 38 个文件

### P1-1. 删除旧 Store（5 个）

- `[ ]` `src/stores/appStore.ts` — 旧主 store，无活跃引用
- `[ ]` `src/stores/panelStore.ts` — 旧面板 store，无活跃引用
- `[ ]` `src/stores/themeStore.ts` — 旧主题 store（13 主题 + visualMode），无活跃引用
- `[ ]` `src/stores/contextMenuStore.ts` — 旧上下文菜单 store，无引用
- `[ ]` `src/stores/pomodoroStore.ts` — 旧番茄 store，仅被死代码引用

### P1-2. 删除旧布局组件（4 个）

- `[ ]` `src/components/layout/PanelSwitcher.tsx`
- `[ ]` `src/components/layout/SidePanelShell.tsx`
- `[ ]` `src/components/layout/TimelineLayout.tsx`
- `[ ]` `src/components/layout/TimelineToolbar.tsx`

### P1-3. 删除旧时间轴视图（4 个）

- `[ ]` `src/components/timeline/ConnectionLines.tsx`（被 TimelineConnections 替代）
- `[ ]` `src/components/timeline/GanttTimelineView.tsx`（如不迁移则删除）
- `[ ]` `src/components/timeline/NarrativeView.tsx`（如不迁移则删除）
- `[ ]` `src/components/timeline/TimelineScrollbar.tsx`

### P1-4. 删除旧效果/主题/功能组件（11 个）

- `[ ]` `src/components/effects/AmbientGlow.tsx` / `FilmGrain.tsx` / `GradientGrid.tsx`
- `[ ]` `src/components/theme/ThemeProvider.tsx` / `ThemeSwitcher.tsx`
- `[ ]` `src/components/mode/VisualModeSwitcher.tsx`
- `[ ]` `src/components/onboarding/Onboarding.tsx`
- `[ ]` `src/components/search/SearchDialog.tsx`（被 CommandPalette 替代）
- `[ ]` `src/components/shortcut/ShortcutHelpPanel.tsx`（被 ShortcutSettings 替代）
- `[ ]` `src/components/structure/StoryStructurePanel.tsx`（被 story-templates.ts 替代）
- `[ ]` `src/components/characters/CharacterRelationshipGraph.tsx`（被 RelationshipGraph 替代）

### P1-5. 删除旧写作/番茄/自动保存组件（10 个）

- `[ ]` `src/components/writing/FocusMode.tsx` / `FocusModeEntry.tsx` / `NameGenerator.tsx` / `DailyGoalSettings.tsx`
- `[ ]` `src/components/pomodoro/PomodoroPanel.tsx` / `PomodoroFloatingTimer.tsx` / `usePomodoroTimer.ts`
- `[ ]` `src/components/auto-save/AutoSaveStatus.tsx` / `RecoveryDialog.tsx` / `useAutoSave.ts`

### P1-6. 删除旧 Hook 和 Lib（7 个）

- `[ ]` `src/hooks/useAnime.ts` / `useLocateOnTimeline.ts`
- `[ ]` `src/lib/animations.ts` / `cinematic-effects.ts` / `micro-interactions.ts`（迁移后删除）
- `[ ]` `src/lib/consistency-check.ts` / `name-generator.ts`

### P1-7. 删除旧历史/通用组件（2 个）

- `[ ]` `src/components/history/useHistoryManager.tsx`（需重新实现）
- `[ ]` `src/components/common/EmptyState.tsx`

---

## P2 — 一致性问题（新旧并存）

> 完整清单详见 [前端问题识别与修复任务](./前端问题识别与修复任务.md) P3 部分

### P2-1. 统一 Store 定义

- `[ ]` 删除旧 themeStore，统一用 useThemeStore（2 主题 light/dark）
- `[ ]` 删除旧 appStore，统一用 useWorkspaceStore
- `[ ]` 统一 ViewMode 定义（用 'statistics' 非 'stats'，含 'relationship'）
- `[ ]` 统一 PanelType 定义（用 'worldview' 非 'worldbuilding'，含 'properties'/'event-editor'）
- `[ ]` 统一焦点模式定义（用 useUIStore.toggleFocusMode）

### P2-2. 动画系统统一

- `[ ]` 迁移所有 animejs 引用（31 处）到 Framer Motion
- `[ ]` 移除 animejs 依赖（package.json 第 36 行）

### P2-3. SideNav/StatusBar 修复

- `[ ]` SideNav 添加 consistency 和 shortcuts 入口
- `[ ]` StatusBar 接入 AutoSaveStatus 或实现保存状态管理（替换硬编码"已保存"）

---

## P3 — 依赖问题

> 完整清单详见 [前端问题识别与修复任务](./前端问题识别与修复任务.md) P4 部分

- `[ ]` 移除 animejs 依赖（package.json 第 36 行 `"animejs": "^4.4.1"`）
- `[ ]` @types/d3 从 dependencies 移到 devDependencies
- `[ ]` 评估是否移除 d3-force（d3 包已包含 d3-force）

---

## P4 — 未来功能（锦上添花）

### P4-1. 单元测试与集成测试

vitest 已在 devDependencies 中，但全项目零测试文件。

### P4-2. CI/CD 流水线

### P4-3. 响应式优化

### P4-4. 导入/导出 UI

**现状**：后端已有 `GET /export` 和 `POST /import` 接口，ImportDialog/ExportDialog 组件已存在但未接入。

### P4-5. 崩溃恢复 UI

**现状**：后端 auto-save.ts 有备份/恢复逻辑，前端 RecoveryDialog 已存在但未接入。

---

## Phase A-D — 基于竞品调研的优化计划

> 基于对 Aeon Timeline、云岸写作、橙瓜码字、百灵创作、墨者写作、51mazi 事序图等竞品的深度调研

### Phase A — 核心差异化（P0）

#### A-1. 多轨道事序图视图
- **竞品参考**：Aeon Timeline Subway 视图、51mazi 事序图
- **描述**：Gantt 风格并行时间线，横轴=时间，纵轴=故事线索/角色，展示多角色/多线索并行发展
- **涉及文件**：迁移 `src/components/timeline/GanttTimelineView.tsx` 到新 store 后接入 MainCanvas

#### A-2. 叙事顺序分离
- **竞品参考**：Aeon Timeline Narrative 视图
- **描述**：支持将事件按"故事叙述顺序"重新排列，与时间顺序独立
- **涉及文件**：`shared/types.ts` 新增 narrativeOrder 字段，迁移 `src/components/timeline/NarrativeView.tsx` 到新 store

#### A-3. 自定义日历系统
- **竞品参考**：Aeon Timeline
- **描述**：支持非地球时间系统（如修仙界 360 天/年、奇幻世界 19 时/日、自定义纪元名称）
- **涉及文件**：`src/lib/custom-calendar.ts`（已存在），修改 `TimelineRuler.tsx` 支持自定义刻度

### Phase B — 智能辅助（P1）

#### B-1. 一致性检查引擎
- **竞品参考**：唐库 Agent
- **描述**：检测角色出现在不可能位置、战力崩坏、时间矛盾、设定冲突
- **涉及文件**：`src/lib/consistency-check.ts`（已存在但仅被死代码引用），`src/components/consistency/ConsistencyPanel.tsx`（需迁移到新 store 后接入）

#### B-2. 伏笔"有伏必应"看板
- **描述**：全局视图展示所有伏笔的埋下/回收状态，未回收伏笔高亮警告
- **涉及文件**：`src/components/foreshadowing/ForeshadowingBoard.tsx`（已存在）

#### B-3. AI 辅助一致性审查
- **描述**：利用 AI 上下文自动检测设定矛盾，生成审查报告

### Phase C — 体验提升（P2）

#### C-1. 电子地图
- **竞品参考**：云岸写作
- **描述**：可视化世界地理，关联事件和角色位置

#### C-2. 多视图系统
- **竞品参考**：Aeon Timeline 7 视图
- **描述**：时间线/地铁图/叙事/关系/大纲/思维导图 6 视图切换（V7 已实现 2 视图：timeline + relationship）

#### C-3. 移动端适配
- **描述**：响应式设计 + 灵感快速捕获

### Phase D — 生态完善（P3）

#### D-1. 导入/导出增强
- **描述**：JSON/PDF/Markdown 多格式导入导出
- **涉及文件**：`src/components/workspace/ExportDialog.tsx`（已存在，需接入）

#### D-2. 插件系统
- **描述**：社区扩展机制

#### D-3. 云端同步
- **描述**：WebDAV/自建服务器同步

---

## 下一任 AI 建议入手顺序

### 第一批（P0 阻塞性，必须修复）

1. **接入 RelationshipView 到 MainCanvas**（最简单，已用新 store）
2. **迁移并接入 EventEditorDialog 到 ContextPanel**
3. **迁移并接入 5 个面板**（characters/worldview/foreshadowing/connections/consistency）
4. **迁移并接入 4 个视图**（outline/narrative/gantt/statistics）
5. **TopToolbar 新建/保存按钮接入功能**
6. **TimelineCanvas 订阅 scrollToEventId**
7. **重新实现 useHistoryManager 并挂载到 AppShell**
8. **handleSave 调用实际保存 API**

### 第二批（P1 清理，删除死代码）

- 删除 38 个残留旧文件
- 移除 animejs 依赖

### 第三批（P2 一致性）

- 统一 PanelType/ViewMode 定义
- StatusBar 接入保存状态

### 验证标准

修复完成后需通过：
- [ ] `npx tsc -b --noEmit` 无错误
- [ ] `npm run build` 成功
- [ ] `npm run dev` 启动成功
- [ ] 6 个视图全部可切换且有实际内容
- [ ] 7 个面板全部可打开且有实际内容
- [ ] 双击事件卡打开编辑器
- [ ] 新建事件按钮可用
- [ ] 保存按钮调用实际 API
- [ ] 命令面板搜索事件后时间轴滚动定位
- [ ] undo/redo 有历史记录可操作
- [ ] 无 animejs import 残留
- [ ] 无旧 store import 残留

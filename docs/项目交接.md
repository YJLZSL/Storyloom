# Storyloom 项目交接文档

> 本文档面向接替开发的 AI Agent。阅读本文档后，你应该能在 10 分钟内理解项目全貌、已知问题和待办事项。

---

## 1. 项目概述

**Storyloom** 是一款面向小说/剧本创作者的时间轴管理桌面应用。用户可以在时间轴上创建事件、角色、伏笔、世界观设定，并通过关联线梳理故事逻辑。应用内置 AI 助手，可读取工作区数据辅助创作。

- **技术栈**：Tauri 2.x + React 19 + TypeScript + Vite 6 + Tailwind CSS v4 + TDesign React + Fastify 5 + SQLite (better-sqlite3) + Drizzle ORM
- **历史技术栈**：项目于 v1.3.0 从 Electron 42 迁移至 Tauri 2.x（Electron 相关内容仅作为历史记录保留）
- **版本**：v1.0.0 正式版（全面重构后的稳定版本）
- **仓库**：https://github.com/YJLZSL/Storyloom
- **工作目录**：`D:\AIKFCC\Storyloom`
- **Node 版本**：20.x LTS（通过 Node.js 官方安装包或 nvm 管理）

---

## 2. 项目结构（核心文件速查）

```
D:\AIKFCC\Storyloom
├── package.json                # 依赖 + Tauri 构建配置
├── vite.config.ts              # Vite 构建配置
├── tsconfig.json               # 前端 TypeScript 配置
├── tsconfig.server.json        # 后端 TypeScript 配置
├── src-tauri/                  # Tauri 2.x Rust 主进程
│   ├── src/
│   │   ├── main.rs             # 主进程入口（run()）
│   │   └── lib.rs              # 命令注册 + sidecar 启动 + 日志
│   ├── Cargo.toml              # Rust 依赖（tauri + plugins + tokio）
│   ├── tauri.conf.json         # Tauri 配置（窗口 / bundle / updater）
│   ├── icons/                  # 应用图标（多尺寸 PNG + ICO + ICNS）
│   └── sidecars/               # sidecar 可执行文件（storyloom-sidecar.exe）
├── src/                        # 前端源码
│   ├── App.tsx                 # 根组件
│   ├── main.tsx                # 前端入口
│   ├── index.css               # ~2600 行，含主题系统 + 动画 + 玻璃拟态 + 交互工具类
│   ├── lib/
│   │   ├── icons.ts            # IconPark 图标统一导出（约 60 个图标）
│   │   ├── ai-config.ts        # AI 配置本地存储（localStorage）
│   │   └── ai-icons.tsx        # AI 厂商品牌 SVG 图标（7 个）
│   ├── components/
│   │   ├── layout/             # AppShell, TopToolbar, LeftPanel, ContextPanel, StatusBar
│   │   ├── timeline/           # TimelineCanvas, TimelineTrack, TimelineEventCard, TimelineRuler, TrackManagerDialog
│   │   ├── ai-panel/           # AIPanel, AIConfigPanel, AIMessage, AIInput, AIConversationList
│   │   ├── settings/           # SettingsDialog, SettingsTabs, AISettingsTab, ShortcutSettings, ThemeSelector
│   │   ├── events/             # EventEditorDialog, EventDetailView
│   │   ├── workspace/          # WorkspaceSelector, CreateWorkspaceDialog
│   │   └── ...
│   └── stores/                 # Zustand 状态管理
├── server/                     # 后端源码
│   ├── index.ts                # Fastify 服务器入口
│   ├── sidecar-entry.ts        # Tauri sidecar 专用入口
│   ├── db/
│   │   ├── index.ts            # 数据库连接 + 迁移（硬编码 DDL 兜底）
│   │   └── schema.ts           # Drizzle ORM 表定义（21 表 + AI 相关表）
│   ├── routes/                 # API 路由
│   │   ├── ai.ts               # /api/ai/* — AI 代理路由（7 个厂商）
│   │   └── ...
│   ├── services/
│   │   └── ai-proxy.ts         # AI 代理核心（7 个提供商）
│   └── lib/
│       └── validation.ts       # Fastify schema 校验
├── shared/
│   └── types.ts                 # 前后端共享类型定义
├── release/                     # 构建产物（历史 Electron 产物 + 当前 Tauri 产物）
│   └── latest.json              # Tauri updater 元数据（旧版 latest.yml 为 Electron 遗留）
└── docs/                        # 本文档
```

---

## 3. 已完成的修改（v1.0.0）

### 全新美术架构
- [x] **三层渲染模型**：交互层 / 内容层 / 渲染层，PixiJS v8 渐进增强背景渲染
- [x] **动画层架构**：GSAP 引擎封装 + 10 种预设 + React Hooks
- [x] **渲染层架构**：PixiBackground 组件，8 种粒子预设（织线/星空/萤火/墨滴/棱镜/落樱/气泡/极光）
- [x] **主题系统 v2.0**：8 套完整主题配置（颜色/字体/动画/材质），运行时动态切换
- [x] **LoomSplash 启动动画**：织机 SVG 动画，GSAP Timeline 4 阶段驱动
- [x] **PageTransition 页面过渡**：10 种过渡预设（淡入淡出/滑动/缩放/旋转/弹性弹出）
- [x] 全面清理 10+ 无用文件和未使用导入
- [x] TypeScript 类型检查：0 错误
- [x] vitest 单元测试：193 / 193 全部通过

### AI 深度集成（v1.3.0）
- [x] AI 对话历史持久化：从 localStorage 迁移到 SQLite 数据库
- [x] AI 工作区上下文注入：自动加载当前工作区的角色、事件、伏笔数据作为 system prompt
- [x] AI 辅助创作增强：事件续写、角色对话、伏笔回收、一致性检查
- [x] 三段式消息结构（系统提示词 + 工作区上下文 + 对话历史）支持 KV 缓存优化
- [x] `aiConversations` / `aiCache` 数据库表已创建并接入后端 CRUD 路由

### 前端 UI 全面修复（v1.1.0 + v1.1.1 + v1.2.x）
- [x] 顶部导航栏响应式布局：CSS 变量 `--toolbar-height`、`--toolbar-tab-height`
- [x] 左侧边栏响应式宽度：`w-60 lg:w-72 xl:w-80`
- [x] 7 个主视图空状态统一使用 `EmptyState` 组件
- [x] 新增 `TrackManagerDialog`，支持轨道显隐/颜色/重命名/删除
- [x] 隐藏轨道恢复按钮放大并修复点击事件（stopPropagation + z-index）
- [x] `TimelineTrack.tsx` 中 `TButton` 统一使用原生 button 替代
- [x] AISettingsTab 使用真实品牌 SVG 图标（7 个厂商）
- [x] 模型卡片添加 `min-w-[200px]`、`truncate` 与 Tooltip
- [x] SettingsTabs 使用 Framer Motion `AnimatePresence` 切换动画
- [x] ShortcutSettings 改为紧凑卡片布局，适配 1280px
- [x] AI 面板标题栏与按钮尺寸统一
- [x] PomodoroTimer 重新设计：环形 SVG 进度环 + 时间+模式标签
- [x] TopToolbar 右侧工具栏全面重做：四组独立容器 + 图标增大 + 按压反馈
- [x] 左侧边栏工具项图标增大（18px/20px）、按钮增大（40px）、圆角升级
- [x] 轨道操作按钮：hover 显示组、图标 16px、红色删除警示
- [x] 全局 CSS 增强：btn-lift / card-hover-shadow / icon-btn / tool-group 工具类

### 后端增强
- [x] `shared/types.ts` 扩展 `AIChatRequest.provider` 为 7 个厂商
- [x] `server/lib/validation.ts` 扩展 provider enum
- [x] `server/db/schema.ts` 新增 `aiConversations` 和 `aiCache` 表
- [x] `server/db/index.ts` 硬编码 DDL 兜底 + 兼容列检测
- [x] `server/services/ai-proxy.ts` 扩展为 7 个厂商 + `listModels` 函数
- [x] `server/routes/ai.ts` 新增 `/models` 和 `/workspace-context` 端点
- [x] `src/services/ai-stream.ts` 传递 provider/apiKey/model 到后端

---

## 4. 已知问题与待办

### v1.3.0 已修复
- [x] AI 对话历史持久化（v1.3.0 核心功能）
- [x] AI 工作区上下文注入（v1.3.0 核心功能）
- [x] 工作区删除失败（bookmarks 表 DDL 兜底缺失）
- [x] 创建轨道失败（数据库完整性修复）
- [x] 翻译键缺失（workspace.manageTitle / panels.bookmarks / panels.maps）
- [x] 字体全局适配不彻底（TDesign font-family 覆盖）
- [x] 右键菜单缺少书签/地图入口

### 仍待解决
- **P0 Bug 已修复（代码层面）**：ErrorBoundary 已添加、API 竞态已修复、Store 死代码已清理、启动流程已优化。需要构建验证。
- **前端重构（进行中）**：CSS 瘦身（2921→~1800 行）、主题去重、布局响应式修复、原生 confirm() 替换。详见交接文档。
- **美术精致化**：硬编码颜色替换为主题令牌、WorkspaceCard 视觉增强、EmptyState 动画提升。
- **状态栏小屏幕截断**：`StatusBar.tsx` 小屏幕下内容被截断
- **自动更新旧版本引导**：v1.1.4 之前的用户需手动下载安装
- **Sidecar 构建限制**：由于当前构建环境 Node v24 不支持 `pkg` 打包工具（pkg 最高支持 Node 18），且无 MSVC BuildTools，无法重新构建 sidecar。因此 v1.1.0 安装包中的 sidecar 仍为旧版本，不包含后端修复（health 27 表检查、工作区删除、端口扩展）。这些后端修复将在环境就绪后重新打包。

### 后续方向（v1.1.0+）
- **v1.1.0**：P0 修复 + VN 编辑器前端 + 前端重构 + 质量提升（进行中）
- **v1.2.0**：导出系统（WebGAL / ZIP / JSON）
- **v1.3.0**：全文搜索 + 版本历史 + 微交互升级
- **v2.0.0**：远期方向（插件系统 / 多语言 / 云同步）
- 详见 [`docs/路线图.md`](./路线图.md)

### ⚠️ 当前开发进度（v1.1.0 WIP）
**已完成的代码变更尚未提交**。接手后请先：
1. 阅读 [`docs/_archive/handoff-2026-06-22/HANDOFF-v1.1.0-WIP.md`](./_archive/handoff-2026-06-22/HANDOFF-v1.1.0-WIP.md) 了解详细进度
2. `git add -A && git commit -m "chore(v1.1.0): P0 Bug修复 + 后端修复 + 文档大整理"`
3. 继续前端重构（CSS 瘦身 → 布局修复 → 视觉精致化 → OutlineView 拆分）
4. `npm run typecheck && npm run test` 验证
5. 构建并发布 v1.1.0

---

## 5. 技术约束（⚠️ 必须遵守）

### 5.1 图标使用规范
**TDesign Button 的 `icon` prop 不兼容 IconPark 图标！** 必须始终使用 children 模式：
```tsx
// ✅ 正确
<TButton><ZoomOutIcon /></TButton>

// ❌ 错误 — 会导致 TypeScript 报错或运行时异常
<TButton icon={<ZoomOutIcon />} />
```

### 5.2 构建环境
- Windows 环境，使用 Git Bash（无 PowerShell）
- Tauri 构建产物：`npm run tauri:build`（产物约 ~50MB，使用系统 WebView2）
- 构建顺序：`npm run build && npm run build:server && npm run build:sidecar && npm run tauri:build`
- **Sidecar 打包**：`npm run build:sidecar` 使用 `pkg` 将 Node.js 后端打包为 `src-tauri/sidecars/storyloom-sidecar.exe`

### 5.3 GitHub Release 文件名
Tauri 默认产物名含空格：`"Storyloom Setup X.Y.Z.exe"`，可通过手动重命名或配置修改为连字符版。

### 5.4 数据库迁移
- 不使用 drizzle 的迁移文件（`drizzle/` 目录不存在）
- 迁移逻辑在 `server/db/index.ts` 的 `runMigrations()` 和 `ensureSchemaCompatibility()` 中
- 新增表必须同时修改 `schema.ts` + `index.ts` 的硬编码 DDL + `ensureSchemaCompatibility` 的列检测

### 5.5 Tauri 自动更新机制
- Tauri 使用 `tauri-plugin-updater` + `.sig` 签名文件
- 更新元数据为 `latest.json`（Electron 时代的 `latest.yml` 为历史遗留产物）
- 构建后检查 `src-tauri/target/release/bundle/nsis/` 下的 `.exe` 和 `.sig` 文件
- **签名密钥**：私钥保存在 `docs/_archive/signing-keys/signing-keys-v5.key`，**切勿上传 GitHub**
- 发布时必须设置环境变量 `TAURI_SIGNING_PRIVATE_KEY`（详见该目录下的 `README.md`）
- 公钥已配置在 `src-tauri/tauri.conf.json` 的 `plugins.updater.pubkey` 中

---

## 6. 快速启动命令

```bash
# 进入工作目录
cd D:\AIKFCC\Storyloom

# TypeScript 严格检查
npm run typecheck

# 运行测试
npm test

# 构建前端
npm run build

# 构建后端
npm run build:server

# 构建 Sidecar
npm run build:sidecar

# 构建 Tauri 桌面应用
npm run tauri:build

# 完整构建（Tauri 时代）
npm run build && npm run build:server && npm run build:sidecar && npm run tauri:build
```

---

## 7. 测试数据

测试运行结果：`193/193 passing`，`~18s`（`22 test files`）。

---

## 8. 相关文档

- [编码指南](./agents.md)
- [路线图](./路线图.md)
- [技术架构](./架构设计.md)
- [快速开始](./快速开始.md)
- [v1.1.0 开发交接](./_archive/handoff-2026-06-22/HANDOFF-v1.1.0-WIP.md)

---

*本文档最后更新：2026-06-22（v1.0.0 统一稳定版）*
